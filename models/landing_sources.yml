version: 2

sources:
  - name: landing
    database: dbt_project_retail  
    schema: landing  
    tables:
      - name: orders
        freshness:
          warn_after:
            count: 365
            period: day
        loaded_at_field: loaded_at_field
      - name: products
      - name: reviews
      - name: users